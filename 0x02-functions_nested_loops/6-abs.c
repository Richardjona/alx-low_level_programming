#include "main.h"

/**
 * _abs - a function that computes the absolute value
 * of integers
 * @n: the argument fo int
 *
 * Return: absolute
 */
int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (-n);
	return (0);
}
